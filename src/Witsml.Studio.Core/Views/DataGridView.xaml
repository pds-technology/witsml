<UserControl x:Class="PDS.Witsml.Studio.Core.Views.DataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="600">
    
    <UserControl.Resources>
        <xcdg:DataGridCollectionViewSource x:Key="GridData" Source="{Binding DataTable}" />
    </UserControl.Resources>
    
    <Grid>
        <xcdg:DataGridControl
            CellEditorDisplayConditions="None"
            ItemsSource="{Binding Source={StaticResource GridData}}">
            <xcdg:DataGridControl.View>
                <xcdg:TableflowView UseDefaultHeadersFooters="False">
                    <xcdg:TableflowView.FixedHeaders>
                        <DataTemplate>
                            <xcdg:ColumnManagerRow />
                        </DataTemplate>
                    </xcdg:TableflowView.FixedHeaders>
                </xcdg:TableflowView>
            </xcdg:DataGridControl.View>
        </xcdg:DataGridControl>
    </Grid>
</UserControl>
